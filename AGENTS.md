# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v54.0.0/ before writing any code
（このプロジェクトは実機Expo Go互換のためSDK 57→54へダウングレード済み。`package.json`の`"expo"`値で実際のSDKバージョンを必ず確認すること）。
